The following Branch is used as a testing area for the implementation of different pipelines

The code "UseMe" goes through a video frame by frame, after both camera distortion and birds eye transformation has been applied.

Note that both the .py files in the subfolders are disfunctional without data, which can be found in main.
	- They are primarly included to show what code has been used to generate the .npz files.